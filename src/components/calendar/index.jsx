/*import * as calendar from "./utils";
import Calendar from "react-calendar";
import { useState } from "react";
import { formatDate } from "../../utils/formatDate";

export function BookingCalendar({ bookings }) {
  const [dateFrom, setDateFrom] = useState();
  const [dateTo, setDateTo] = useState();




  return (
    <div className="">
      <Calendar className="font-paragraphs w-full h-full border p-10 mt-10 mb-10" minDate={new Date()} maxDate={maxDate} view="month" onClickDay={onClickDay} tileClassName={tileClassName} prevLabel={<span className="px-2 pb-1 me-2 border font-bold">«</span>} nextLabel={<span className="px-2 pb-1 ms-2 border font-bold">»</span>} prev2Label="" next2Label="" />
    </div>
  );
}
*/
